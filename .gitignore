# The Shopify theme is a SEPARATE, independent Git repository — never track it here.
theme/

# Secrets — never commit
.env
.env.*
*.key
.mcp.json

# Non-AI-OS local source material / large binaries (not architecture)
AuroomWellness/
VeldoraHavenItemPictures/
VeldoraHavenLogoPictures/
Röshults/
Brand/Images/
Brand/Logos/
Create Claude Agent Skill.html
Create Claude Agent Skill_files/
*.xlsx

# Nested repos — tracked separately
VeldoraHaven1/

# OS / editor noise
*.log
.DS_Store
Thumbs.db
