# archive/ — Historical Record (L4)

**Responsibility:** completed tasks, superseded decisions, and previous implementations
kept for reference. TTL: indefinite. Read-mostly.

Rules: nothing here is authoritative for current work — it is history. Move items here
from [`../state/`](../state/) when finished, and from [`../decisions/`](../decisions/)
when superseded (leave a forward link). Do not delete history without user authorization.
